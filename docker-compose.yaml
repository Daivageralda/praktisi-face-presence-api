services:
  web:
    build: .
    ports:
      - "8888:8888"  # Sesuaikan dengan settings.API_PORT kamu
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    env_file:
      - .env
    restart: always
